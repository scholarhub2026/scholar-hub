export type TInQueryFormValues = {
  name: string;
  email: string;
  phoneNumber: string;
  message: string;
  subject: string;
  place: string;
  status?: string; // Optional for status updates
};
