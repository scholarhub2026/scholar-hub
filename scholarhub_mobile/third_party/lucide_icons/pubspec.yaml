name: lucide_icons
description: Vendored + patched lucide_icons (0.257.0) — plain IconData (no
  extends), Dart 3 SDK. Names identical to the original.
version: 0.257.1
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
flutter:
  fonts:
    - family: Lucide
      fonts:
        - asset: assets/lucide.ttf
